import 'package:flutter/material.dart';
import 'home_page.dart';

class AppSingleRender extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
